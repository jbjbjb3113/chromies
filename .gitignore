node_modules/
.env
.env.local
.env.*.local
.ponder/
ponder-env.d.ts
# Ponder codegen output lives at the repo root only (contracts/generated/
# holds real, committed Solidity source -- e.g. ChromaPaletteData.sol -- and
# must not be swept up by this rule).
/generated/
dist/
coverage/

# Generated art renders (regenerated via art-pipeline/generate.js + gallery.js)
art-pipeline/output/

# Local backups (not for the repo)
_backups/

/lib/

# Foundry (broadcast JSONs embed RPC URLs — never commit)
cache/
out/
broadcast/
**/broadcast/**
run-latest.json
.verify-worktrees/
.verify-tmp-*/
.verify-worktree/
