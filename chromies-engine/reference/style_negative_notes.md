# Normies — negative similarity reference only

Do **not** copy, trace, or train on Normies artwork.

This folder stores **fingerprints only** (hashes, masks, face vectors) computed from externally managed reference images. Those images never live in this repository.

Use `compare_to_normies.py` (Phase 5) against `normies_fingerprints.json`.
